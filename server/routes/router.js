const express = require('express');

const router = express.Router();
/* here we want to do all the routes for the api
 to clear the server.js */

// not gonna use it because it is a simple project

module.exports = router;
